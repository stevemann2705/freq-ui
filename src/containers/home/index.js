import React from 'react';
import HeaderMenu from "../../components/header";

const HomeContainer = () => {
    return (
        <div>
            <HeaderMenu />
        </div>
    );
};

export default HomeContainer;
